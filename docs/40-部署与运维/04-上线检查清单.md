# 04 上线检查清单

> 上线前必须逐项验证。任何一项不通过都不能上线。

## 安全

- [ ] **默认账号密码已修改**：`admin/admin123` → 强密码
- [ ] **JWT_SECRET 已替换**：`openssl rand -base64 64` 生成两段不同的串
- [ ] **.env 文件未提交到 Git**：`server/.env` 在 .gitignore 内
- [ ] **数据库密码已替换**：seed 用的默认密码不在线上用
- [ ] **SSL 证书有效**：certbot 签的 cert + 自动续期已配置
- [ ] **数据库端口不对外**：`bind 127.0.0.1`（pg_hba.conf）
- [ ] **SSH 端口 + 密钥登录**：禁用密码登录

## 数据

- [ ] **数据库已迁移**：`npm run prisma:migrate deploy` 成功
- [ ] **种子数据已写**：seed 完成后立即修改默认账号
- [ ] **备份策略已配**：每日凌晨 pg_dump 到独立目录 + 异地
- [ ] **迁移前已快照**：重要节点先做一次全量备份

## 服务

- [ ] **PM2 进程拉起**：`pm2 status` 显示在线
- [ ] **开机自启**：`pm2 startup` + `pm2 save` 已配
- [ ] **Nginx 配置已测**：`nginx -t` 无报错
- [ ] **SSL 验证**：`https://yourdomain.com` 浏览器打开无证书警告
- [ ] **健康检查**：`curl /api/admin/dashboard/overview` 返回 401（未登录）

## 监控

- [ ] **日志切割**：`logrotate` 已配（pm2-logrotate 或系统 logrotate）
- [ ] **磁盘监控**：剩余空间 < 20% 告警
- [ ] **进程监控**：PM2 进程挂了自动拉起

## 备份与回滚

- [ ] **回滚方案已演练**：
  - 服务端：`git checkout` + `pm2 reload`
  - 数据库：`pg_restore`（演示版没自动化）
  - 静态资源：直接从对象存储 / 备份拉

## 业务

- [ ] **关键流程 e2e 走通**：
  - 客户端：领券 → 下单（用券）→ 付款 → 发货 → 收货 → 评价
  - 客户端：申请退款 → 后台批准 → 标记已退款 → 券退回 + 库存退回
  - 后台：导出订单 xlsx → 浏览器下载
- [ ] **库存预警列表**有数据展示（id 22~24 应触发预警）
- [ ] **权限隔离**：`operator` 账号访问 `audit:view` / `setting:edit` 应被拒绝

## 文档

- [ ] **AGENTS.md §7 坑位**：是否还有遗漏的演示版假设
- [ ] **README 默认账号表**：更新为生产账号
- [ ] **操作日志查询**：试查最近 10 条确认审计正常