# 02 账号权限与登录

> 后台登录页、账号体系、权限码生效方式、自定义指令、Pinia 持久化。

## 1. 模块定位

后台账号体系独立于前台用户：

- 独立表 `admin_users` + `admin_roles`
- 独立 JWT secret（`JWT_ADMIN_SECRET`）
- 独立登录入口（`/login`）
- 通过 `@RequirePermission('xxx:yyy')` 控制接口权限
- 通过 `v-permission` 控制前端按钮显隐

## 2. 涉及数据表

| 表 | 用途 | 详见 |
| --- | --- | --- |
| `admin_users` | 后台管理员账号 | `10-服务端/03-数据库Schema说明.md` |
| `admin_roles` | 后台角色 + 权限码数组 | 同上 |

## 3. Server 接口清单

文件：`server/src/admin-auth/admin-auth.controller.ts`

| 方法 | 路径 | 权限 | 说明 |
| --- | --- | --- | --- |
| `POST` | `/api/admin/auth/login` | `@Public()` | 登录，返回 `{ token, user }` |
| `POST` | `/api/admin/auth/logout` | （仅需登录） | 占位实现；JWT 无状态，前端清 token 即可 |
| `GET` | `/api/admin/auth/profile` | （仅需登录） | 返回当前管理员信息（含权限码） |

### 登录 DTO（`server/src/admin-auth/dto/login.dto.ts`）

```ts
export class AdminLoginDto {
  @IsString() username!: string
  @IsString() password!: string
}
```

### 登录返回结构

```ts
interface AdminLoginResult {
  token: string
  user: {
    id: number
    username: string
    nickname: string | null
    avatar: string | null
    role: string                // Role.code，如 'SUPER_ADMIN'
    permissions: AdminPermission[]   // 权限码数组，'*' 表示通配
  }
}
```

### 登录失败抛错

- `UnauthorizedException('账号或密码错误')` — 用户不存在或密码错误
- `ForbiddenException('账号已被禁用')` — `AdminUser.status !== 1`

## 4. Admin 路由与页面

| 路由 | 文件 | 权限码 |
| --- | --- | --- |
| `/login` | `admin/src/views/LoginView.vue` | — |
| `/dashboard` | `admin/src/views/DashboardView.vue` | `dashboard:view` |
| `/users` | `admin/src/views/UserListView.vue` | `user:list` |
| `/categories` | `admin/src/views/CategoryListView.vue` | `category:list` |
| `/products` | `admin/src/views/ProductListView.vue` | `product:list` |
| `/orders` | `admin/src/views/OrderListView.vue` | `order:list` |
| `/banners` | `admin/src/views/BannerListView.vue` | `banner:list` |
| `/announcements` | `admin/src/views/AnnouncementListView.vue` | `announcement:list` |

## 5. 登录页（LoginView.vue）

要点：

- 表单：用户名 + 密码（无验证码）
- 提交：`useAdminAuthStore().login({ username, password })` → 成功后写 localStorage + Pinia，弹「成功」toast，按 `redirect` query 跳转（默认 `/dashboard`）
- 失败：弹「账号或密码错误」toast
- 提交期间按钮显示「登录中...」loading

## 6. 账号 store（admin-auth.ts）

文件：`admin/src/stores/admin-auth.ts`

```ts
export const useAdminAuthStore = defineStore(
  'admin-auth',
  () => {
    const token = ref<string>(localStorage.getItem('admin-token') ?? '')
    const user = ref<AdminUser | null>(readUser())

    const isLoggedIn = computed(() => !!token.value)
    const permissions = computed<string[]>(() => user.value?.permissions ?? [])

    function hasPermission(p: string): boolean {
      if (!user.value) return false
      if (permissions.value.includes('*')) return true   // 超管通配
      return permissions.value.includes(p)
    }

    async function login(payload) {
      const res = await api.adminLogin(payload)
      token.value = res.token
      user.value = res.user
      localStorage.setItem('admin-token', res.token)
      localStorage.setItem('admin-user', JSON.stringify(res.user))
      return res
    }

    async function logout() {
      try { await api.adminLogout() } catch { /* ignore */ }
      token.value = ''
      user.value = null
      localStorage.removeItem('admin-token')
      localStorage.removeItem('admin-user')
    }

    async function fetchProfile() {
      user.value = await api.adminProfile()
      localStorage.setItem('admin-user', JSON.stringify(user.value))
    }

    return { token, user, isLoggedIn, permissions, hasPermission, login, logout, fetchProfile }
  },
  {
    persist: { key: 'admin-auth', paths: ['token', 'user'] }
  }
)
```

要点：

- **初始化**：先从 `localStorage` 读 token / user（兼容刷新页面）
- **持久化**：`pinia-plugin-persistedstate` 把 `token` + `user` 写到 `localStorage` 的 `admin-auth` key
- **hasPermission**：`*` 直接放行，否则精确匹配权限码
- **logout**：先调 `api.adminLogout()`（失败也清本地），再清 token + localStorage

## 7. 自定义指令 v-permission

文件：`admin/src/directives/permission.ts`

```ts
mounted(el, binding) {
  const auth = useAdminAuthStore()
  if (!auth.isLoggedIn) { hide(el); return }
  if (binding.value === undefined) return

  const allowed = Array.isArray(binding.value)
    ? binding.value.some((p) => auth.hasPermission(p))
    : auth.hasPermission(binding.value)

  if (!allowed) hide(el)
}

function hide(el) { el.parentElement?.removeChild(el) }
```

> ⚠️ 该指令只在 `mounted` 时检查一次。如果权限码在运行期变化（例如调用 `fetchProfile` 后），需要手动重新渲染或刷新页面。当前实现适用于权限基本不变的场景。

## 8. 路由守卫

文件：`admin/src/router/index.ts`

```ts
router.beforeEach((to) => {
  const auth = useAdminAuthStore()
  const requiresAuth = to.meta.requiresAuth !== false

  if (requiresAuth && !auth.isLoggedIn) {
    return { name: 'login', query: { redirect: to.fullPath } }
  }
  if (to.name === 'login' && auth.isLoggedIn) {
    return { name: 'dashboard' }
  }

  const perm = to.meta.permission
  if (typeof perm === 'string' && !auth.hasPermission(perm)) {
    return { name: 'dashboard' }
  }

  if (typeof to.meta.title === 'string') {
    document.title = `${to.meta.title} · 管理后台`
  }
})
```

## 9. Layout 与菜单

文件：`admin/src/layouts/AdminLayout.vue`

菜单项（与路由 meta 同步）：

```ts
const menuItems = computed<MenuItem[]>(() => [
  { path: '/dashboard',     title: '仪表盘',     icon: 'House',    permission: 'dashboard:view' },
  { path: '/users',         title: '用户管理',   icon: 'User',     permission: 'user:list' },
  { path: '/categories',    title: '分类管理',   icon: 'Menu',     permission: 'category:list' },
  { path: '/products',      title: '商品管理',   icon: 'Goods',    permission: 'product:list' },
  { path: '/orders',        title: '订单管理',   icon: 'List',     permission: 'order:list' },
  { path: '/banners',       title: '轮播图管理', icon: 'Picture',  permission: 'banner:list' },
  { path: '/announcements', title: '首页公告',   icon: 'Bell',     permission: 'announcement:list' }
])
```

要点：

- 每项菜单也加了 `v-permission`，没权限自动隐藏
- 顶栏右侧：主题切换下拉（浅色 / 深色 / 跟随系统）+ 用户头像下拉（个人信息 / 退出登录）
- 退出登录：`ElMessageBox` 二次确认 → 调 `auth.logout()` → 跳 `/login`

## 10. 默认账号

| 用户名 | 密码 | 角色 | 权限 |
| --- | --- | --- | --- |
| `admin` | `admin123` | `SUPER_ADMIN` | 所有（含 `*`） |
| `operator` | `admin123` | `ADMIN` | 日常运营权限（不含删除） |

详见 `10-服务端/06-数据迁移与Seed脚本.md`。

## 11. 相关文档

- 权限码体系：`10-服务端/04-认证与权限体系.md`
- 数据库表：`10-服务端/03-数据库Schema说明.md`
- 各业务模块：`03` ~ `07` 章节
- 开发约定 / FAQ：`00-总览与入门/04-开发约定与常见问题.md`
